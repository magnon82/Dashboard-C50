/** Group / holding label (shell eyebrow when BrandLogo already shows the venue). */
export const PRODUCT_CLUSTER = 'Cluster Culinario';

/** Venue brand — used in logo alt, login, meta, and legal copy. */
export const PRODUCT_VENUE = 'Carranza 50';

/** Full org line when Carranza 50 is not already on screen (login, meta, docs). */
export const PRODUCT_ORG = `${PRODUCT_CLUSTER} · ${PRODUCT_VENUE}`;

/** Main product description / category name */
export const PRODUCT_NAME = 'Business Management Suite';

/** Short Spanish supporting line */
export const PRODUCT_TAGLINE = 'Sistema integral de gestión empresarial';

export const PRODUCT_META_TITLE = `${PRODUCT_NAME} · ${PRODUCT_VENUE}`;

export const PRODUCT_META_DESCRIPTION = `${PRODUCT_NAME} — ${PRODUCT_TAGLINE}. Tablero de negocio, Master Control y operación completa · ${PRODUCT_ORG}.`;
